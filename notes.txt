Mission 1
 * Could use same dataset as below


Mission 2
 * Create a dataset to train a YOLO model to recognize paper plates
   - Take videos of paper plates(many frames per second)
   - Need to consider lighting/environment changes

Fly drone to target?? --> are we responsible for this

1/10/26
For both missions, create a model that takes in input of grayscale images, and identifies
if there is a plate-shaped object in the image.

For image 2 to recognize if a target has been hit yet(contains blue), first recognize a
plate with the first model, and then create a second model to identify if the target is
blue(has been hit)
